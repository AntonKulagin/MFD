import characters from './fake.api/characters.api'
import location from './fake.api/location.api'
import episodes from './fake.api/episode'

const API = {
  characters,
  location,
  episodes,
}

export default API
